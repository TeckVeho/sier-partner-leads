# プロジェクトコンテキスト：地方SIerパートナー候補パイプライン

このファイルは Cowork が単独で読んで作業を開始できるように書かれている。
過去のチャット履歴を前提にしないこと。

## 背景・目的

弊社はダイセーSDC（群馬県高崎市の地方SIer）と提携し、開発案件を受託している。
SDCの特徴は、地域密着の営業力で中小企業の基幹システム案件を元請で獲得できる一方、
COBOL/オフコン/VB6等のレガシー技術は扱えるが、AI開発のノウハウが無いこと。

戦略は「第二・第三のSDC」を見つけ、案件供給を安定させること。そのために、
SDCと同じ特徴（老舗・地域密着・元請・レガシー技術・AI未対応）を持つ企業を
継続的に発掘するパイプラインを構築している。このファイルはその運用マニュアル。

## アーキテクチャ（3つのパーツ）

1. **スキル** `sier-partner-screening`（`~/.claude/skills/sier-partner-screening/`に配置済み）
   ICP判定基準・スコアリングロジック・過去の落とし穴の知見が入っている。
   判定作業をするときは必ずこのスキルの `SKILL.md` を読むこと。

2. **リポジトリ** `sier-partner-leads`（このファイルが置かれている場所）
   候補データの実体。`data/candidates.csv`が正データ、`candidates.md`が表示用。

3. **Cowork**（あなた自身）
   毎日スケジュール実行され、①の基準で②を更新し、必要ならSlack通知する実行主体。

## 現在の設定値

| 項目 | 値 |
|---|---|
| 対象都道府県 | 群馬・栃木・茨城 |
| Slack通知先 | `D01BG830F9U`（自分自身へのDM） |
| 既存パートナー（競合回避対象） | ダイセーSDC（群馬県高崎市） |
| Slack通知の閾値 | legacy_score + 加点合計が7点以上 |

対象県を増やす、閾値を変える等の変更は、この表とスキルの`references/data-sources.md`
の両方を更新すること。

## 日次パイプラインの全体フロー

```
① 情報源チェック     → references/data-sources.md の各ソースを確認
② ICPスクリーニング   → SKILL.md のルールベース基準で判定
③ 重複排除して追記    → scripts/git_append.py が実行
④ commit & push       → 追加が1件以上のときだけ
⑤ 高スコア候補の深掘り → スコア7+のみ、実サイトを自分で読み直す
⑥ Issue作成           → ⑤で問題なければ「要確認」ラベルで作成
⑦ Slack通知           → ⑥の内容を要約して送る。0件の日は送らない
```

⑤〜⑦が最も重要な工程。①〜④はキーワードマッチのルールベースなので、
実は結構誤判定する（過去の実例：茨城県の「茨城計算センター」は社名パターン上は
最有力候補だったが、実サイトを読むとAWS認定パートナーで除外すべき企業だった）。
**⑤で必ず自分の目でサイトを読み直すこと。ルールベースの判定を鵜呑みにしない。**

## 具体的な実行手順

### Step 1: 既知の企業を把握する

`data/candidates.csv`を読み、(企業名, 都道府県)の組み合わせで既知企業のセットを作る。
このセットに含まれる企業は、除外判定であっても再取得・再スコアリングしない
（`data/candidates.csv`は「候補」だけでなく「除外済み」も含めて全件保持する設計）。

### Step 2: 新規企業を探す

`sier-partner-screening`スキルの`references/data-sources.md`に記載の情報源
（特に対象3県の情報サービス産業協会名簿）から、Step 1のセットに無い企業を探す。

### Step 3: ICP基準でスクリーニングする

`SKILL.md`の必須条件A1〜A5、加点条件B1〜B8、除外条件に沿って判定する。
判定ロジックの詳細（キーワード辞書等）は`references/scoring.md`と
`scripts/score_site.py`を参照。

**重要**：情報が空欄・サイト取得失敗の場合は「候補」と判定せず、
verdictを「要確認」とすること。空欄を条件を満たしたと解釈しない。

### Step 4: リポジトリへ追記する

判定結果を一時CSV（列: name, pref, url, subtype, legacy_score, modern_score, verdict）
にまとめ、以下を実行する。

```bash
python scripts/git_append.py --candidates <一時CSVのパス> --repo .
```

追加件数が1件以上なら：

```bash
git add -A
git commit -m "候補更新: $(date +%F) 新規N件"
git push
```

追加件数が0件ならコミットしない。

### Step 5: 高スコア候補を深掘りする

Step 4で新規追加された候補のうち、legacy_score + 加点合計が7点以上のものについて、
実サイトを自分で直接読み直す。確認すること：

- A4（モダン技術の記述がない）が、空欄放置による誤判定でないか
- A型（受託・何でも屋）かB型（業種特化パッケージベンダー）かのサブタイプ
- 群馬の候補の場合、SDCとの顧客層の競合（金融系・公共系・組込み系以外は要注意）

除外すべきと判断したら、`data/candidates.csv`の該当行のverdictを更新して終了する
（Issue化・Slack通知はしない）。

### Step 6: Issue作成とSlack通知

候補として問題なければ：

1. GitHub Issueを作成し、ラベル`要確認`を付ける（企業名をタイトルに）
2. Slack（`D01BG830F9U`）に通知する。フォーマット：

```
:mag: 新規パートナー候補（要確認）

*{企業名}*（{都道府県}・{市区町村}）
サブタイプ: {A型/B型} / スコア: {点数}点

{深掘りで分かった判断根拠を1-2行}

Issue: {Issueへのリンク}

※最終判断は商談前に確認してください
```

**「確定候補」であるかのような書き方をしないこと。** 最終判断は人間が商談前に行う。

## やってはいけないこと

- `candidates.md`を直接編集しない。必ず`git_append.py`経由で追記する
- 群馬の候補で、SDCと同一顧客層（中小企業向け基幹系の何でも屋）のものを
  Issue化・Slack通知しない。金融系・公共系・組込み系は例外的に可
- 情報が不足している企業を、確認をスキップして「候補」と判定しない
- 該当候補が0件の日にSlack通知しない（空振り通知が続くと無視されるようになる）
- Slack通知やIssue本文に、確度を誇張した表現（「即受注確実」等）を使わない

## 詰まったときの参照先

- ICP判定基準そのものを見直したい → `sier-partner-screening/SKILL.md`
- 情報源を追加・変更したい → `sier-partner-screening/references/data-sources.md`
- キーワード辞書を調整したい → `sier-partner-screening/references/scoring.md`
- スケジュールタスクの文言テンプレート → `scripts/COWORK_PROMPTS.md`
